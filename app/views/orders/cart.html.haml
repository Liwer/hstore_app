- @order.each_with_index do |p, i|
  %strong
  = link_to "Remove product", rm_product_path(product_id: i)
  %br
  = p['name']
  %strong
  Total Price
  = p['price']
  %br
  - p['options'].each_with_index do |o, ii|
    - o.each do |k,v|
      = "#{k}: #{v}"
      %br
    %br  
      = link_to 'add_count', option_count_path(product_id: i, option_id: ii, change: 'plus')
      %br
      = link_to 'rm_conut', option_count_path(product_id: i, option_id: ii, change: 'minus')
    %br
    %strong
    = link_to "rmOption", rm_option_path(product_id: i, option_id: ii)
    %br
Cart Price
= @cart_price
%br
  %br
  %hr
- session[:cart].each_with_index do |c, i|
  = c
  %br
  = i
  %br
